import 'package:flutter/material.dart';

class WordChainScreen extends StatelessWidget {
  const WordChainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kelime Zinciri'),
      ),
      body: const Center(
        child: Text('Yakında...'),
      ),
    );
  }
} 